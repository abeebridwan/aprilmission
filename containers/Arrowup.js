/* eslint-disable react/prop-types */
import ArrowUp from '../components/arrowup/index';

const ArrowUpContainer = ({ children }) => (
  <ArrowUp>
    I am gonna be the greatest
    {children}
  </ArrowUp>
);

export default ArrowUpContainer;
